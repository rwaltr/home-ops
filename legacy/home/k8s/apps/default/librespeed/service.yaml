---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: librespeed
  name: librespeed
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: librespeed
  type: ClusterIP
